import { NgZone } from '@angular/core';
import { Observable } from 'rxjs';
import { GeoPoint } from '../../interface/geo-point';
import { MapsApiWrapper } from '../maps-api-wrapper';
import { NgMapsPolyline } from '../../directives/polyline';
export declare abstract class PolylineManager<T = any> {
    protected _mapsWrapper: MapsApiWrapper;
    protected _zone: NgZone;
    protected _polylines: Map<NgMapsPolyline, Promise<T>>;
    constructor(_mapsWrapper: MapsApiWrapper, _zone: NgZone);
    protected _convertPoints(line: NgMapsPolyline): Array<GeoPoint>;
    abstract addPolyline(line: NgMapsPolyline): any;
    abstract updatePolylinePoints(line: NgMapsPolyline): Promise<void>;
    abstract setPolylineOptions(line: NgMapsPolyline, options: {
        [propName: string]: any;
    }): Promise<void>;
    abstract deletePolyline(line: NgMapsPolyline): Promise<void>;
    abstract createEventObservable<E>(eventName: string, line: NgMapsPolyline): Observable<E>;
}
